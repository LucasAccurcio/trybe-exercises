const uppercase = (string) => string.toUpperCase();

const firstLetter = (string) => {
  for (let c of string) {
    return c;
  }
}

const concatena = (a, b) => a.concatena(b);

module.exports = { uppercase, firstLetter, concatena };
